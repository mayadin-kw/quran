# التحفيظ — البنية الداخلية

## البيانات المعاد استخدامها

- `PAGE_MAP` هو المرجع للـ `verse_key` بصيغة `surah:ayah` ولمواضع النص داخل `quran.txt`.
- `quran.txt` هو مصدر النص العثماني المعروض؛ لا تُنسخ آيات في نظام التحفيظ.
- قائمة القرّاء وروابط EveryAyah تأتي من `window.QuranAppData` التي يعرّضها `app.js`.

## المحرك

`memorization.js` يملك حالة مستقلة عن القارئ العادي. المراحل الأساسية هي التلقين الظاهر، الترديد الظاهر، التلقين غيباً، الترديد غيباً، الربط الداخلي، ربط الآيات، إعادة التعلم، والإكمال. لا يبدأ ربط آيتين قبل إكمال جميع مقاطع الآية الحالية.

الآية التي تتجاوز أربع كلمات تقسم بصورة حتمية إلى مقاطع من 2–4 كلمات تقريباً مع إبقاء الحرف القصير اللاحق مع المقطع السابق. كل مقطع يحتفظ بـ `surah` و`ayah` و`segmentIndex` وموضع كلماته و`verseKey`.

## التحقق الصوتي

واجهة `QuranRecitationRecognizer` موجودة في الواجهة. GitHub Pages ثابت ولا يشغّل Whisper/PyTorch، لذلك لا يوجد ادعاء بأن التحقق الحقيقي متصل. تسجل الواجهة بـ `MediaRecorder` عند ضغط المتعلم للميكروفون، ثم يمر التسجيل إلى الـ adapter. لربط خدمة ذاتية الاستضافة، يستبدل `check()` بطلب `POST /api/recitation/check` يحوي التسجيل و`expectedText` والسياق.

المرشح المقترح للخدمة الذاتية هو `Habib-HF/tarbiyah-ai-v1-1`: بطاقة النموذج تصفه كنموذج Whisper Small لقراءة القرآن، مرخص MIT، وحجمه المعلن نحو 967MB؛ ما زال يتطلب اختباراً محلياً فعلياً على أصوات متعلمين قبل اعتماده. البديل `wasimlhr/whisper-quran-v1` أكبر (Whisper Large-v3) وترخيصه CC-BY-NC-4.0، لذا لا يصلح افتراضه في مشروع تجاري. لا يوجد نموذج مضمّن في المتصفح.
